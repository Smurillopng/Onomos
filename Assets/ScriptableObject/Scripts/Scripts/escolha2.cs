using DG.Tweening;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class escolha2 : MonoBehaviour
{/*
    [SerializeField]
    private Transform[] Sequenciacaminho;

    [Range(1.0f, 10.0f), SerializeField]
    private float velo = 1.0f;

    [SerializeField]
    private Ease moveEase = Ease.Linear;

    [SerializeField]
    private DoTweenType tipoDotween = DoTweenType.moverumadirecao;

    private enum DoTweenType
    {
        moverumadirecao
    }

    void Start()
    {

        if (tipoDotween == DoTweenType.moverumadirecao)
        {
            if (Sequenciacaminho == Vector3.zero)
                Sequenciacaminho = transform.position;

            transform.DOMove(Sequenciacaminho, velo);
        }

    }*/
}
